# Used by `npm start` and baked into `npm run build`.
# Local:
REACT_APP_API_URL=http://localhost:5000/api

# Production (set this in Vercel, then rebuild):
# REACT_APP_API_URL=https://to-do-project-react-backend.onrender.com/api

# AI and search keys stay on the server only. Do not add AI_API_KEY or SEARCH_API_KEY here.
